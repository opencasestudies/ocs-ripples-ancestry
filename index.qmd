---
title: "Open Case Studies: The Biology of Ancestry"
output:
  html_document
---

####  {.outline}

![](img/mainplot.png){.lightbox fig-alt="" width="800"}

#### 

:::: disclaimer_block
::: license_box_header
The contents of this case study are solely the responsibility of the authors and do not represent the official views of the Fred Hutch or NIH.
:::
::::

:::: license_block
::: license_box_header
License
:::

This work is published under CC By-NC, you can reuse it for free with attribution, but you can not use the materials for commercial purposes.
::::

:::: reference_box
::: reference_box_header
To cite this case study, please use:
:::

::: inner_block
Kate Isaac, Hanako Osuga, Jeanne Chowning, Wright, Carrie (2026). <https://github.com/opencasestudies/ocs-ripples-ancestry>. The Biology of Ancestry (Version v1.0.0).
:::
::::

To access the GitHub repository to see the source materials and data that went into making the case study, please see here: <https://github.com/opencasestudies/ocs-ripples-ancestry>.

# **Motivation**

------------------------------------------------------------------------

This case study explores the topic of human {{< glossary "genomic variation" >}}, especially with respect to {{< glossary ancestry >}}.

:::::::: definition_box
::::::: panel-tabset
### **Definition**: Genomic Variation

::: definition_box_header
Human Genomic Variation
:::

::: inner_block
Genomics variation refers to DNA sequence differences among individuals within a population or when comparing different populations. Human genomic variation focuses specifically on variation within the human species. Some variants influence biological function (potentially causing disease), while others have no biological effects [@GenomicVariationNIH].
:::

### **Definition**: Ancestry

::: definition_box_header
Ancestry
:::

::: inner_block
Ancestry is a personal understanding of one's genomic heritage - a statement about a person's relationship to other individuals in their genealogical history. Ancestry is often self-reported by an individual and is a personal concept focusing on the how or the process of their family history rather than a societal pattern [@Yudell2016].
:::
:::::::
::::::::

To explore these topics, we'll first ask questions such as "What is the human genome?", "What is genomic variation?", and "Why should we care about human genomic variation?". Data representing genomic variation and ancestry will be discussed and ways to represent or visualize this data will be highlighted. In addition, ways to work with data will be compared and contrasted, examining the pros and cons of techniques that either utilize more manual approaches or more  processing through computational work. Since the data we will be working with is not large, we can use these techniques in any case.

## What is the human genome?

The human {{< glossary genome >}} (all of an individual's {{< glossary DNA >}}) is made of stretches of {{< glossary nucleotides display = "nucleotides (with bases A,C,G,T)" >}}. The nucleotides ultimately encode for {{< glossary protein >}} products that function in the body in important ways. Proteins include enzymes like amylase to break down food, peptide hormones such as insulin to regulate blood sugar, and structural proteins such as keratin in skin and nails. An individual's genome encodes all the proteins they need for their body to function normally.

```{r, echo = FALSE, message=FALSE, warning=FALSE, out.width="100%"}
ottrpal::include_slide("https://docs.google.com/presentation/d/1fmPgoGk8KCZvRQx9ljjti8Nn44JdA2G2yGm1BwmL7V8/edit?slide=id.p#slide=id.p")
```

The Central Dogma is a theory describing how genetic information is stored and how it flows. DNA stores genetic information within the genome. DNA produces {{< glossary RNA >}} through a process called {{< glossary transcription >}}. Specific regions of the genome called {{< glossary genes >}} are transcribed into RNA molecules. RNA produces proteins through a process called {{< glossary translation >}}.

```{r, echo = FALSE, message= FALSE, warning = FALSE, out.width="100%"}
ottrpal::include_slide("https://docs.google.com/presentation/d/1fmPgoGk8KCZvRQx9ljjti8Nn44JdA2G2yGm1BwmL7V8/edit?slide=id.g3e81f594328_0_10#slide=id.g3e81f594328_0_10")
```

The human genome is diploid meaning that it has two copies of each of the 23 distinct {{< glossary chromosome display = "chromosomes" >}} (1, 2, 3, ..., 21, 22, sex chromosome / X or Y) for a total of 46 chromosomes. The copies are not identical because they are inherited from different parents -- one from each. These chromosomes are housed in the nucleus of the cell. Chromosomes have DNA wound around specialized proteins called histones forming dense coils to compactly store the genomic information. Some regions of the chromosome are more tightly wound than others. DNA replication and transcription occur in the loosely wound/more accessible regions [@Pathak_Bordoni_2026].

Recent work from the T2T project [@Nurk_etal_2022] completely sequenced the Human Genome and reported that

::: notice
Note that the terms "nucleotide" and "base" are often used interchangeably since the base is a part of the nucleotide.
:::

- it is 3.055 billion–base pairs or nucleotides in length.
- there are 63,494 genes in the human genome (only 19,969 of which code for proteins).
- 53.94% of the genome is made up of repetitive DNA.

Much of the human genome contains non-coding DNA instead of genes. This isn't to say that the DNA in these non-coding regions is doing nothing. Structural elements such as telomeres (which protect the end of chromosomes) are part of the non-coding regions. Regulatory elements are part of these non-coding regions as well. While they are not included in eventual protein products, they regulate when and how much RNA is produced in transcription.

See the expandable section below for further resources to review the Central Dogma.

::: emphasis_block
<details>

<summary>Further Resources for the Central Dogma</summary>

This [Khan Academy lesson](https://www.khanacademy.org/science/ap-biology/gene-expression-and-regulation/translation/a/intro-to-gene-expression-central-dogma) provides an overview of the Central Dogma as well as the transcription and translation processes [@khanacademy_centraldogma].

{{< video https://www.youtube.com/watch?v=TatNfCyQ52Q >}}

</details>
:::

## What is genomic variation?

Most of a person's genome is the same as another person's. All humans need the same proteins to function. But very small parts of our genome, especially in the areas that do not code for proteins, can vary across individuals. This genomic variation is caused by changes in the nucleotide sequences that can cause differences in how a protein functions or the amount of protein created. Some variations can change the function of a protein so severely that it stops doing its intended function, while others can increase or decrease the amount of protein that is produced.

These variations in a genome are caused by mutations. Mutations are any changes in the DNA or nucleotides that make up the genome.

::: notice
Mutation is just a term for any change to the DNA. Not all mutations are deleterious or bad. Many have no impact at all and some can be beneficial.
:::

These variations can be caused by a change of a single nucleotide ("point mutations") or changes involving more than one nucleotide. An example of a single nucleotide change would be changing a base from C → T or adding or removing a single nucleotide (pronounced "snip").

```{r, echo = FALSE, message=FALSE, warning=FALSE, out.width="100%"}
ottrpal::include_slide("https://docs.google.com/presentation/d/1fmPgoGk8KCZvRQx9ljjti8Nn44JdA2G2yGm1BwmL7V8/edit?slide=id.g3c938ed4e06_0_9#slide=id.g3c938ed4e06_0_9")
```

If a point mutation occurs fairly often within a population and is observed with a common frequency, it is termed a single-nucleotide polymorphism or SNP (pronounced "snip").

Sometimes mutations involve more than one nucleotide. An example of a mutation involving more than one nucleotide would be if a chunk of DNA, like ACCGT, were to be repeated and become ACCGTACCGT (such changes with more than 50 nucleotides are called **structural variants**). Some large structural changes (like the Translocation shown in the figure below) may even occur across multiple chromosomes (inter-chromosomal) rather than being within a single chromosome (intra-chromosomal).

```{r, echo = FALSE, message=FALSE, warning=FALSE, out.width="100%"}
ottrpal::include_slide("https://docs.google.com/presentation/d/1fmPgoGk8KCZvRQx9ljjti8Nn44JdA2G2yGm1BwmL7V8/edit?slide=id.g3c938ed4e06_0_28#slide=id.g3c938ed4e06_0_28")
```

## How do nucleotide changes happen?

Variation can be introduced into the genome through *mutation* or *recombination* during *meiosis*. Mutations may occur due to errors during the *replication* of genomic material during cell division, imperfect repair mechanisms not catching mistakes, environmental factors or *mutagens* that damage genomic material (e.g., high-energy radiation or certain chemicals or viruses), or spontaneous/random changes.

::::: definition_box
::: definition_box_header
Mutations
:::

::: inner_block
Changes to the DNA that can either be a single nucleotide change, larger changes involving segments of nucleotides, or in some cases changes to other chemical modifications of the DNA that change how tightly it is wound. See this [article](https://www.nature.com/scitable/topicpage/dna-is-constantly-changing-through-the-process-6524898/) for more information. 
:::
:::::

No matter the cause of mutation, not all variations will be passed on to offspring/children. Mutations may be somatic (occurring in cells that are not part of the reproductive system) or germline (occurring in cells that are part of the reproductive system). Germline mutations and meiotic recombination are examples of *heritable* variation that will be passed on to future generations.

{{< video https://youtu.be/vl6Vlf2thvI?si=gAvQY0jJFMu1yCNt >}}

```{=html}
<!--
Add Berkeley resource: https://evolution.berkeley.edu/dna-and-mutations/types-of-mutations/
-->
```

```{=html}
<!--
  Add definition box with tabs for recombination, meiosis, replication, mutagens, and heritable
-->
```

Human genomes are large (over 3 billion nucleotide base pairs)! And most of the genome is identical when comparing two individuals or an individual to a *reference genome*. On average, if you compared the genomes of two individuals, there would be \>99% matching between the two. This is often visualized with a *dotplot* where highly identical genomes will have a dense, diagonal line with some off-diagonal dots (or lines for larger variants like an insertion or duplication).

```{=html}
<!--
  Add definition box with tabs for reference genome and dotplot
-->
```

```{r, echo = FALSE, message = FALSE, warning = FALSE, out.width="100%"}
ottrpal::include_slide("https://docs.google.com/presentation/d/1fmPgoGk8KCZvRQx9ljjti8Nn44JdA2G2yGm1BwmL7V8/edit?slide=id.g3e81f594328_0_32#slide=id.g3e81f594328_0_32")
```

::: emphasis_block
<details>

<summary>More details about dotplots</summary>

Besides comparing two human samples to each other, dotplots are also used to compare a human sample to a reference genome, reference genomes to each other, human samples or reference genomes to other species, or really any other species/sample comparison.

Comparisons for less related samples produce more complex dotplots that represent larger structural variants like the inversions in the following example.

```{r, echo = FALSE, message = FALSE, warning = FALSE, out.width="100%"}
ottrpal::include_slide("https://docs.google.com/presentation/d/1fmPgoGk8KCZvRQx9ljjti8Nn44JdA2G2yGm1BwmL7V8/edit?slide=id.g3e81f594328_0_44#slide=id.g3e81f594328_0_44")
```

</details>
:::

## Why should we care about human genomic variation?

Especially if there's so much similarity, why should we care about human genomic variation? Not all variations or mutations have an impact. Some variations can be “functional” and lead to changes not just in the genome, but also in the *proteins* that are *translated* from the genetic material. However, not all variation is “functional”; some changes don't impact the proteins that are translated from the genetic material. (See silent mutations below).

Functional changes, on the other hand, may impact how much of a protein is made, the protein structure, and therefore its eventual role in the *cell*, or where and when the protein is expressed due to changes in parts of the genome that help regulate gene transcription. Certain diseases may be caused or worsened by genetic variation. Cancer is a common example of this, where genes that control cell growth, division, or scheduled cell death are mutated, and their proteins do not function properly to control the cell cycle, leading to uncontrolled cell growth that develops into tumors.

For other diseases, a protein may not be expressed at the correct levels or the protein may be folded incorrectly because of its mutated structure, leading to inefficiencies in or the inability of the protein to perform its function(s). This is the case in the mutation that leads to sickle-cell anemia and changes in the hemoglobin protein. To protect against this, there are biological redundancies that act as a safety net. These create backup systems that can mitigate issues by compensating for or taking the place of nonfunctional components. But even with redundancies and safety nets, mutations can lead to small changes with major consequences.

```{=html}
<!--
  Add definition box with tabs for functional, proteins, translated, cell
-->
```

:::: emphasis_block
<details>

<summary>Different types of (single-nucleotide) mutations</summary>

As mentioned earlier, there are a few different types of mutations. Single-nucleotide polymorphisms are single-base pair differences, while larger structural mutations change more than one nucleotide. Within the group of single-nucleotide mutations, there are different types that are defined based on the effect of the mutation.

::: panel-tabset
### Summary

**Single-nucleotide changes**

Different types of single-nucleotide mutations are defined based on the effect of the mutation.

- **Silent mutations** have an underlying nucleotide change, but the *amino acid* building blocks of the protein remain unchanged. Not only does the protein remain unchanged, but for a true "silent" mutation, there is no effect on the cell.
- **Synonymous mutations**: Synonymous mutations have an underlying nucleotide change, and the amino acid building blocks of the protein remain unchanged. They differ from silent mutations, however, because these have an impact on the cell.
- **Missense mutations** result in an amino acid substitution, which can impact protein structure and function.
  - Some missense mutations are **“conservative”** where the amino acid substitution is switching one amino acid for another similar one with similar biochemical properties (e.g., size, charge, etc.).
  - A **“non-conservative”** missense mutation switches an amino acid to one with dissimilar biochemical properties – this is likely to have a greater impact on protein structure and function than a conservative missense mutation.
- **Nonsense mutations** introduce an early *stop codon*, halting translation and producing a truncated, likely nonfunctional protein.

**Single-nucleotide insertions or deletions (indels)**

A single nucleotide insertion or deletion (AKA an "indel") may introduce a **frameshift mutation**. Frameshift mutations often impact the structure and function of proteins because of the shifting of the reading frame during translation for all subsequent codons after the insertion or deletion.

{{< video https://www.youtube.com/watch?v=xYOK-yzUWSI&t=162s >}}

### Comparison

| Mutation Type | Single Nucleotide Change | Amino Acid Change | Possible impact on cell due to change in protein abundance or structure |
|:-------------|:------------:|:------------:|:------------------------------:|
| Silent | Substitution |  | No Impact |
| Synonymous | Substitution |  | ✅ |
| Missense | Substitution | ✅ | ✅ |
| Nonsense | Substitution | Truncation | Probably large impact due to likely non-functional protein |
| Frameshift | Addition or Loss | All following shift are changed | Likely large impact |

: Single Nucleotide Mutations Summary Table

### Silent

Silent mutations have an underlying base pair change (i.e., a C to an A), but the amino acid building blocks of the protein remain unchanged. Since these mutations do not change the encoded amino acid, they are called silent. However, these silent mutations may still impact how often a protein is produced because certain tRNA anticodons are less available or may take longer to process in translation; so while they code for the same amino acid, they may not be truly "silent" and may have an effect in the cell. Such mutations are called synonymous mutations.

### Missense

Missense mutations result in an amino acid substitution, which can impact protein structure and function. The single nucleotide change altered the amino acid that was encoded by that segment of DNA. This causes a substitution in the amino acid encoded for in that part of the protein. Some missense mutations are "conservative" where the amino acid substitution is switching one amino acid for another similar one with similar biochemical properties (e.g., size, charge, etc.). A "non-conservative" missense mutation switches an amino acid to one with dissimilar biochemical properties - this is likely to have a greater impact on protein structure and function than a conservative missense mutation.

{{< video https://www.youtube.com/watch?v=LoxOmFoQOQk >}}

### Nonsense

Nonsense mutations introduce an early stop codon - halting translation prematurely. A stop codon typically signals the end of a segment of RNA that encodes a protein, signaling the end of translation. An early stop codon will prematurely stop the production of the protein and result in a shorter (or truncated), often non-functional protein.

{{< video https://www.youtube.com/watch?v=Qe05MxqNlJk >}}

### Frameshift

A single nucleotide insertion or deletion (also known as an "indel"), or an insertion or deletion that is a length not a multiple of three (which is the length of a codon), may introduce a frameshift mutation. Frameshift mutations often impact structure and function to a higher degree than missense mutations because these cause downstream impacts, shifting the reading frame during translation for all subsequent RNA codons. When genetic material is read to create a protein, it is read in three-nucleotide chunks -- the reading frame. A shift in the reading frame can greatly impact how a protein is made.



Here you can see a video about what happens with an insertion at the DNA, mRNA, and amino acid level:
{{< video https://www.youtube.com/watch?v=yv_vnMKdJzY >}}
:::

</details>
::::

## Why do genetic changes sometimes persist in populations?

Genetic variation is considered necessary for survival. Scientists hypothesize that species are under various selective pressures to adapt and retain genetic changes that allow them to survive and reproduce, especially when competing with other species (e.g., predators vs prey, virus-host interactions, etc.) Given this, heritable variation (e.g., recombination during meiosis or germline mutations that are passed from generation to generation) can be beneficial by allowing for the selection of advantageous traits for adaptation leading to better survival. In other words, evolution depends upon the random accidents and mistakes that are mutations being maintained within a population because the changes can be advantageous.

Every person alive inherited genomic material from two individuals, and each of those individuals inherited genomic material from two other individuals, continuing backwards through their family history. Each generation of people pass down genomic materials to the next. Since genomic material is what is passed down from ancestors, genomic material is what is compared when considering ancestry and studying how individuals are related. Individuals who are more closely related will share more genomic material in common than those who are more distantly related.

Additionally, the length of time is important because time is needed for genetic variation to accumulate. The longer the amount of time or number of generations between two individuals, the more genetic variation there will be. With more time and more generations between two individuals, the more opportunities there are for genetic variations to occur and accumulate. **Ancestry** is something that is determined and formed over time and through a process -- the process of genetic material being passed down. Therefore ancestry is defined as a "process-based concept" of how someone is related to other individuals in their genealogical history. Ancestry is a biological concept that captures the process of genomic material being passed down from generation to generation. But ancestry can mean many different things. It can capture the closer relationships between individuals and their ancestors, as well as the deeper history of people or species over thousands and tens of thousands of years, as they migrated, mixed, and moved.



## How can we describe or quantify ancestry?

Ancestry captures a process that occurs over time and generations. When talking about ancestry, it can be complicated to quantify it. One way to do this is to compare the genomic material shared between individuals.

When comparing genomic material between individuals, how similar or identical a sequence is between the two is a quantitative measure describing how much overlap or matching there is between the genomic material. Homology is a qualitative description constructed from the amount of similarity and suggests that the sequences or genomic material share a common ancestor. The idea is that if two sequences look similar, they must have come from the same ancestor and diverged relatively recently, since there are few genetic variations that have accumulated. Models used to compare sequences take into account the different kinds of mutations and how commonly they occur.

When thinking about ancestry, we often think about ancestral groups or populations instead of individuals (ex, what group or community an individual is related to or whether one group is related to another group or community). This also applies to studying similarities and differences in current groups based on their ancestry. For example, evaluating every human's blood pressure on earth would be nearly impossible. It would be much more feasible to study a representative sample of the general population. To create this representative sample, we would need to have individuals who represent different subpopulations within the greater population in our sample or we risk missing important information. Making a representative reference sample is important to make sure the results apply to the general population, and not just the subpopulation that was studied.

To do this, individuals are often grouped based on their ancestry into populations. A population is defined as a group of randomly mating (diploid).Diploid individuals are those with two complete sets of chromosomes (one from each parent) – for example humans. Defining a population is rather subjective. Historically, a lack of migration due to physical boundaries (such as mountains or oceans) influenced population definitions. However, over time, with society overcoming such boundaries, populations that used to be separated have mixed, leading to admixture.

::: definition_box
:::: definition_box_header
Admixture
::::
:::: inner_block
Definition of Admixture
::::
:::


## How do you group humans?

Because of the admixture that has been going on for multiple generations, how populations are partitioned into separate groups is subjective, with no definitive dividing line. An individual's view of their own ancestry will determine what population they identify as. This adds a cultural, religions, and sometimes geographical element to what used to be a strictly biological concept. People can identify with a culture that is more dominant in their life (this can be around race, ethnicity, religion, etc.) as well as where they grew up, as opposed to where their ancestors are from.

**NOTE: Reviewed/Edited background material has only been brought over through here so far**

Additionally, the definition of populations may also differ in scale. As mentioned earlier, ancestry can mean relationships across a couple of generations or tens of thousands of years. The 1000 Genome Project was an international research effort that aimed to create the most detailed reference of human genetic variation available at that time. Populations and superpopulations used by the 1000 Genomes Project that are both examples of creating population-esque categorizations. Superpopulations combine populations together, creating fewer overall categories. These categorizations don't have a single genetic or biological characteristic driving them, but rather are subjectively and arbitrarily defined groups. There are 26 ethnically or geographically defined groups in the 1000 Genomes Project. There are 5 superpopulations that are composite groups focusing on a broader continental definition - not based on an individual's current location, but instead their genetic ancestry. Given the mixing of populations throughout time, there may not be a single clear categorization for an individual into one of these populations or superpopulations.

## Can we use ancestry as a proxy in medical care?

## What does this all mean?

# **Main Questions**

------------------------------------------------------------------------

####  {.main_question_block}

<b><u> Our main question: </u></b>

1)  How can genomic data be used to explore the ancestry of an individual or population?
2)  How does the genetic makeup of different world populations compare?
3)  How can coding and computational biology/data science principles and tools help us investigate our questions?

#### 

# **Learning Objectives**

------------------------------------------------------------------------

<u>**Data Science Learning Objectives:**</u>

1.  Explain the differences and benefits of using R vs Google Sheets to work with data
2.  Describe how data can take different shapes: long and wide
3.  Recode data to change original values, using programming to reduce accidents and to document changes, rather than changing values manually
4.  Crease an ancestry plot (a special heatmap-like column plot that shows differences)
5.  Describe what different elements of the ancestry plot represent
6.  Combine two plots together

<u>**Statistical Learning Objectives:**</u>

1.  Explain how clustering can help identify groups
2.  Summarizing means within a dataset

<u>**Topical Learning Objectives:**</u>

1.  Describe how genomic data shows what human variation looks like
2.  Explain genetic similarities and differences between people
3.  Describe how racial and ancestry groups might differ
4.  Recognize the patterns of genetic variation across continents

------------------------------------------------------------------------

We will begin by loading the packages that we will need:

```{r, message = FALSE, warning = FALSE}
library(here)
library(readr)
library(dplyr)
library(DT)
library(tidyverse)
library(ggplot2)
library(patchwork)
install.packages("ggforce", repos="https://cloud.r-project.org/")
library(ggforce) #for facet_col
```

| Package | Use |
|-------------------------------|----------------------------------------|
| [here](https://github.com/jennybc/here_here){target="_blank"} | to easily load and save data |
| [readr](https://readr.tidyverse.org/){target="_blank"} | to import data files |
| [dplyr](https://dplyr.tidyverse.org/){target="_blank"} | to manipulate data |
| [tidyverse](https://tidyverse.org/){target="_blank"} | to manipulate and visualize data |
| [patchwork](https://patchwork.data-imaginist.com/){target="_blank"} | to combine plots |
| [ggforce](https://ggforce.data-imaginist.com/){target="_blank"} | to extend the plotting capabilities of ggplot2 |

The first time we use a function, we will use the `::` to indicate which package we are using. Unless we have overlapping function names, this is not necessary, but we will include it here to be informative about where the functions we will use come from.

# **Context**

------------------------------------------------------------------------

Provide some more details and background on the case study topic and its relation to public health/the world.

# **Limitations**

------------------------------------------------------------------------

# **What are the data?**

------------------------------------------------------------------------

The data for this case study comes from the authors (primarily Dana R. Al-Hindi) of a commentary [@Carlson_Henn_Al-Hindi_Ramachandran_2022] which expands upon data and a previous analysis presented by a broader team [@Li_etal_2008]. Specifically, for the commentary analysis, a clustering tool (ADMIXTURE) [@Alexander_2009], which also estimates ancestry proportions for each sample, was run on SNPs from \~1900 individuals without providing any prior ancestry information besides the number of clusters that the program should produce. The original analysis used a tool called *frappe* [@Tang2005], specifying 7 clusters for the results, so the commentary analysis specified 7 clusters as well. The original analysis had 938 samples, 86.5% of which had reported ancestry outside of Africa, with the remaining 13.5% having reported ancestry from within Africa. To make the point that the proportion of categories within a sample will impact the end result, the commentary analysis inverted those numbers, using 85% of samples with reported ancestry within Africa and only 15% with reported ancestry outside of Africa. Though a different tool was used by the original analysis to cluster the samples, the tools are very similar in how they work, but ADMIXTURE is faster.

Ultimately they both calculate the proportion of an individual's genetic ancestry that is from a predefined ancestry group. The number of clusters indicates the number of predefined ancestry groups. In this case they chose 7 groups to account for the genetic variation across all of their samples across the world to investigate.  

|   | Original Analysis | Commentary Analysis |
|:---------------------------------|:----------------:|:------------------:|
| Reference | @Li_etal_2008 | @Carlson_Henn_Al-Hindi_Ramachandran_2022 |
| Number of samples | 938 | 1918 |
| Number of populations | 51 | 26 |
| Reported ancestry outside of Africa | 86.5% | 15% |
| Reported ancestry within Africa | 13.5% | 85% |
| Clustering tool | *frappe* | ADMIXTURE |
| Number of clusters | 7 | 7 |

The data provided from the commentary analysis authors lists the reported ancestry/population group for each sample, as well as the resulting estimated proportion of ancestry for that sample for each of the 7 clusters.

The data includes 1913 rows and 9 columns with the following information:

1) **pop:** A column about the population where a sample came from, with values like Egyptian or British.
2) **id:** A column with the European Genome-phenome Archive (EGAN) ID number. This is a way of identifying a person's genomic data. So each row represents a genetic sample from an individual person.
3) **columns labeled k1-k7:** These columns show the proportion of each persons genetic ancestry attributed to 7 divisions of variation. 


If we take a look at a few individual rows of the table, which we will do in a moment, we will see that the proportions in the k columns add up to roughly 1 (there is some rounding in how the data is presented).


## Clustering

Let's take a minute to dive deeper into what these K columns mean. 

Consider that you have a bag of Halloween candy. Perhaps you want to figure out a way to divide that candy into groups so that you vary the kind of candy you eat until you are out of candy. There are actually many ways you could divide the candy by differnt features. For example, you could divide it based on if it contains chocolate or has a fruit flavor, or you could divide it by texture, if it is crunchy or chewy, or perhaps you want to share with your Aunt who only likes candy that isn't too sweet or with your cousin who really like sour candy. As you can see there are many ways in which we could group the candy. 

In order to group people by ancestry, we can group them by their SNPs. As discussed SNP variants may have been introduced through mutation and then may have continued to persist in specific population or may have largely disappeared because of migration patterns. For example, if a population of a small number of people migrated to an isolated geographical location, any rarer SNPs may become more numerous among later generations that are decedent from these original settlers.  

We can try to group people by evaluating which SNP variants co-occur or seem to distinguish different groups. Going back to our candy example, we might find that a cherry flavor often co-occurs with chewy textures. These two features could help us to group our candy. We may have some candy that has a cherry flavor that are not chewy, but the two features together may help to create the optimal number of groups that we are trying to achieve for our candy sharing and pacing problems. We can use math to help us identify a more optimal number of population groups based on our SNPs. Clustering algorithms help us to reduce the mathematical distances of individuals within our groups or in other words, find clusters that have more similar individuals and keep the clusters generally less overlapping. So the data that we have has identified that 7 clusters generally distinguishes the individuals into groups quite well.  Since humans are quite genetically similar, allowing for individuals to overlap across the clusters is helpful. Therefore, as we will see an individual could have a portion of its SNPs overlap more closely with more han one cluster. 




# **Data Import**

------------------------------------------------------------------------

:::{.panel-tabset}

## R

When reading in the data, we want to ensure that we specify the names of the headers so that the first row of data is included within the data itself and not treated as a header. 

```{r, message=FALSE, warning=FALSE}
ancestry_props <-
  readr::read_table(here("data/raw/AGR.without_related_chr10_maf001_LDp50_final_removesmallpops_shrinkBaganda.combined_fam.Q"), col_names = c("pop", "id", paste0("k", 1:7)))

ancestry_props_prelim <- ancestry_props
```

Let's take a look at a few rows now. We can use the `slice` function to get the data for a single row. Say we want the 500th row we can do so like this:

```{r}
slice(ancestry_props, 500) 
```


We can see that most of the genomic variation appears to match up with cluster 3 and 5. these have the largest numbers, with k5 showing a much higher proportion that is pretty close to 1. Therefore most of the genetic variation of this individual is mostly attributable to the 5th cluster, or in other words, this person would fit pretty well with that group based on genetic variation, however, other individuals do not group as easily.

Let's take a look at another row, let's look at the 20th row.

```{r}
slice(ancestry_props, 20) 
```

Now this individual groups more with 2 clusters, with .38 for k3, and .62 for K6. In this case this person doesn't fit as well within an individual cluster, however, the proportion for each of these clusters, still allows us to identify their genetic variation relative to the other individuals. You can see that we could identify more ancestral groups than just 7 based on how much well an individual matches with these groups. 

## Google Sheets

First, navigate to [your Google Drive home](https://drive.google.com/drive/u/0/home) and create a new Google Sheet.

  * Click the `+ New` button in the top left.
  * Select `Google Sheets` from the dropdown list.
  * This will open a blank, "Untitled spreadsheet".
  * Rename the Google sheet something relevant like `Ancestry data` by clicking the "Untitled spreadsheet" name in the top left, selecting the words, and replacing them with your new title.

Next, download the data file with estimated ancestry proportions for each sample with this link: [https://github.com/opencasestudies/ocs-ripples-ancestry/raw/refs/heads/main/data/wrangled/ADMIXTURE_ancestry_props.txt](https://github.com/opencasestudies/ocs-ripples-ancestry/raw/refs/heads/main/data/wrangled/ADMIXTURE_ancestry_props.txt)

  * Note that this file is named `ADMIXTURE_ancestry_props.txt`.
  * Note where this file downloaded on your computer. (Is it in your `Downloads` folder?) You will need to know in order to import it into Google Sheets

Import the data file that you've just downloaded to Google Sheets.

  * Click the `File` option from the Google Sheets menu bar and select `Import` from the dropdown menu.
  * In the `Import file` window that pops open, select the `Upload` tab.
  * Click the blue "Browse" button.
  * Find the `ADMIXTURE_ancestry_props.txt` file that you downloaded in the previous step, and select that to upload.
  * Leave the "Import location" as the "Replace spreasheet option".
  * Switch the "Separator type" from "Detect automatically" to **"Tab"**.
  * Ensure that the "Convert text to numbers, dates, and formulas" option is checked.
  * Click the green "Import data" button on the bottom right of the window.


:::

# **Data Wrangling**

------------------------------------------------------------------------

:::{.panel-tabset}

## R

First let's take a look at our data. In R we can do that by using the `head` function which shows us the top 6 lines of the data. Or we can click on the data from the Environment pane of R Studio if we are using R Studio. 

```{r}
head(ancestry_props)
```

Let's use the `distinct` function to check that we have all the distinct populations we expect. 

```{r}
distinct(ancestry_props, pop) 
```

We want the names of our populations to match that of the paper, we know from the authors what some of these abbreviations stand for and we can tell that some are just slightly different like `Egyptians` vs `Egyptian`. Here we will modify the pop column (also known as variable) to reflect the full name. To do this we need to use the `mutate` function to change the variable and we need the `case_when` function to help us recode those abbreviated values to the full name. We will also include a .default = pop statement so that anything we don't specify stays as it was. 

This is really powerful because instead of going in and replacing every value where these occurred by hand, we can do it very quickly in R and we have a record of how we changed the data.



```{r}
ancestry_props <- ancestry_props %>%
         dplyr::mutate(pop = case_when(
                         pop == "ACB" ~ "African Caribbean",
                         pop == "ASW" ~ "African American",
                         pop == "CHB" ~ "Han Chinese",
                         pop == "Egyptians" ~ "Egyptian",
                         pop == "ESN" ~ "Esan",
                         pop == "GBR" ~ "British",
                         pop == "GIH" ~ "Gujarati",
                         pop == "GWD" ~ "Mandinka",
                         pop == "LWK" ~ "Luhya",
                         pop == "MSL" ~ "Mende",
                         pop == "MXL" ~ "Mexican",
                         pop == "Nama/Khoe-San" ~ "Nama",
                         pop == "RwandeseUgandan" ~ "Rwandese",
                         pop == "uganda_gwas_unknown" ~ NA_character_,
                         pop == "Batooro" ~ NA_character_,
                         pop == "Nyanjiro(Tanzania)" ~ NA_character_,
                         pop == "TSI" ~ "Italian",
                         pop == "YRI" ~ "Yoruba",
                         .default = pop))


```

We can check our work by using the `distinct` function again to see how unique values for `pop` we have now:

```{r}
distinct(ancestry_props, pop)
```

Next, we want to make our data a class called factor because this allows us to order our data in a specific way. We can do so using the `factor` function and we can specify the order of our levels for the populations. 

```{r}

ancestry_props <- ancestry_props %>%
         dplyr::mutate(pop = factor(pop,
                      levels = c("Nama", "Zulu", "Mende", "Esan", "Yoruba", "Mandinka", "African Caribbean", "African American", "Banyarwanda", "Banyankole", "Bakiga", "Barundi", "Rwandese", "Baganda", "Luhya", "Gumuz", "Somali", "Wolayta", "Oromo", "Amhara", "Egyptian", "Gujarati", "Mexican", "British", "Italian", "Han Chinese"))
            ) 

```

Next we want to remove rows where we had NA values. We would always want to be careful about what these mean and if we should remove them. In our case we can't use them to recreate the plot, so we will drop them. Again this step could take quite a bit of time in Excel or Google Sheets (depending on how manually we did it) and we could accidentally remove rows that we don't want to remove!

```{r}

ancestry_props <- ancestry_props %>%
  tidyr::drop_na() # dropping a few samples that had no data

ancestry_props_temp <- ancestry_props
```

Now let's save our clean data using the write. table function. 

```{r}
write.table(ancestry_props, file = here("data/wrangled/ADMIXTURE_ancestry_props.Q"),
            row.names = FALSE,
            quote = FALSE,
            sep = "\t")
```

## Google Sheets

First let's take a look at our data. 

* In the first row, we see the column names `pop`, `id`, `k1`, `k2`, `k3`, `k4`, `k5`, `k6`, and `k7`.
  * The labeled population is in column A
  * The sample ID is in column B.
  * The ancestry proportions are in columns C through I.
* By scrolling, we see that there are 1914 rows of data (including the header), meaning there is data for 1913 individuals.
  * If you didn't want to scroll to see how much data there is, you could enter `=COUNTA(A:A)` into a spreadsheet cell off to the side and it would return the same number. The [`COUNTA` function in Google sheets](https://support.google.com/docs/answer/3093991?hl=en) returns how many values are in a dataset, in this case the A column. 

Using Google Sheets, to work with data instead of just visually inspecting it requires the use of menu tools or using formulas. Formulas may be more difficult to actually implement and read what it is doing, but it is more reproducible for others and yourself who are reviewing your work. Using menu tools is not reproducible given there is no record of what was done and when it was done. `=COUNTA(A:A)` is an example of a Google Sheets formula.

<details><summary>How to build a formula for Google Sheets</summary>

Add some basic tips about writing Google Sheets Formulas

* Uses functions
* Starts with `=`
* Can be nested (but only one `=` )
* Can be all uppercase or all lowercase
* Takes 'arguments' or 'parameters'
* How to write a range
* How to reference a range in a different sheet

Make an image showing a formula and highlighting some of this stuff

</details>


* To see the unique population names in the `pop` column, in another spreadsheet cell off to side, enter `=UNIQUE(A:A)` and it will list the unique population names in that column. 

::: {.question_block}

How would you change the `=UNIQUE(A:A)` function to show the number of unique populations in the dataset?

<details><summary>Hint</summary>

You can put a spreadsheet function inside another spreadsheet function. The function on the outside (the outer function), will be used or evaluated after the function on the inside.

</details>

<details><summary>Answer</summary>

Use `=COUNTA(UNIQUE(A:A))` and that will return the number of unique populations within that column + 1 because it is also counting the `pop` header as a unique value in the A column.

</details>

:::

:::{.emphasis_block}

The **wrangling steps that are being performed in the R version of this case study have already been done for the dataset that was imported into Google Sheets**. However, we will discuss the options you have for performing those sorts of wrangling steps in Google Sheets. 

If you want to try any of the suggestions here just to play around with the concepts, click the downward facing arrow next to the Google Sheet name `ADMIXTURE_ancestry_props` on the bottom banner and click "Duplicate". You can treat the new "Copy of ADMIXTURE_ancestry_props" like a sandbox where you can test some of the data wrangling capabilities of Google Sheets.

:::

### Mutating or Substituting Values

If you needed to "mutate" values in a spreadsheet (in other words, switching out a value / specific pattern that is in the sheet with another value or specific pattern), you could use a combination of spreadsheet functions that enact the "substitute" function for the column and multiple substitutions (but this will not perform an in-place substitution), or you can use the built-in "Find and replace" (from the `Edit` menu) functionality of Google Sheets. The former is complex, both to build and understand, but more reproducible for other people to see what was done. The latter is much easier to implement, but would have to be repeated manually for every needed substitution, and is not reproducible / a set of formulas that you or others can reference / review later.

First, the method using the [`substitute` function](https://support.google.com/docs/answer/3094215?hl=en):

  * `=SUBSTITUTE(text_to_search, search_for, replace_with)` searches a specific Google Sheet cell or range of cells (`text_to_search`) for a specific value or text pattern  (`search_for`) that it will then replace (`replace_with`). While it can search a range, unless combined with [`=ARRAYFORMULA()`](https://support.google.com/docs/answer/3093275?hl=en) or used multiple times, it [will only make a replacement for one cell using "implicit intersection"](https://support.google.com/docs/thread/338433380?hl=en&msgid=338456425) (e.g., by looking at the row in the `text_to_search` range that intersects with the row where the `=SUBSTITUTE` call is).
  * `=ARRAYFORMULA(SUBSTITUTE(text_to_search, search_for, replace_with))` will make replacements for the whole `text_to_search` range.
  *  Combining several more functions allows you to make multiple substitutions across a range of data.

:::{.emphasis_block}

:::{.panel-tabset}

#### Single Substitution

Instructions for trying `SUBSTITUTE` for a single substitution:

* In the `COPY of ADMIXTURE_ancestry_props` sheet, click the down arrow next to the "A" for the "A" column and select `"+ Insert 1 column right"`.
* Enter `=SUBSTITUTE(A1:A1914, "pop", "Test") in B1 and B4. 
* Notice that B1 shows `"Test"` (because A1 has the value "pop"), and B4 shows `"Amhara"` (because A4 does not have the value "pop"). The rest of the cells in the B column are blank.
* Enter `=SUBSTITUTE(A1:A1914, "Zulu", "Test")` in B5 and B50.
* Notice that B5 shows `"Amhara"` (because A5 does not have the value "Zulu"), and B50 shows `"Test"` (because A50 has the value "Zulu"). The rest of the cells in the B column (besides B1 and B4) are blank.
* Enter `=SUBSTITUTE(A1:A1914, "Amh", "test")` in B3.
* Notice that B3 shows `testara` (because A3 has the value `"Amhara"`) and substitute replaces the `Amh` part with `test'.

#### Single across a range

Instructions for trying `ARRAYFORMULA` & `SUBSTITUTE` for a single substitution:

* In the `Copy of ADMIXTURE_ancestry_props` sheet, click the down arrow next to the "A" for the "A" column and select `"+ Insert 1 column right"`. 
* Enter `=ARRAYFORMULA(SUBSTITUTE(A1:A1914, "pop", "Test"))` in B1. 
* Notice that column B autofills, copying column A except for the first value which changes from `"pop"` to `"Test"`. 
* Click cell B1 and edit the formula in the formula bar, switching `"pop"` to `"Amhara"` and press enter / return.
* Notice that the column now switches `"Test"` back to `"pop"` in the first row, and every occurrence of `"Amhara"` switches to `"Test"`. 

#### Multiple across a range

Instructions for combining several more functions allows you to make multiple substitutions across a range of data:

* Click the `+` (Add Sheet) button on the bottom left of the Google Sheet.
* Name the new sheet "Pop Index".
* Set up some test substitutions in the new "Pop Index" sheet.
  * In Column A enter:
    * "African Caribbean" in A1
    * "Han Chinese" in A2
    * "Egyptian" in A3
    * "Esan" in A4
    * "Amh" in A5
  * In Column B enter:
    * "ACB" in B1
    * "CHB" in B2
    * "Egyptians" in B3
    * "ESN" in B4
    * "test" in B5
* Navigate back to the sheet "Copy of ADMIXTURE_ancestry_props".
* Click the down arrow next to the "A" for the "A" column and select `"+ Insert 1 column right"`.
* Copy the following nested formula, double click in B1 (so that the cursor is in the formula bar), paste the nested formula, and press enter / return.

```{.default}
=ARRAYFORMULA(REDUCE(A1:A1914, 
                     SEQUENCE(ROWS('Pop Index'!A1:A5)), 
                     LAMBDA(current_list, 
                            row_num, 
                            SUBSTITUTE(current_list, 
                                       INDEX('Pop Index'!A1:A5, row_num), 
                                       INDEX('Pop Index'!B1:B5, row_num)
                                      )
                            )
                    )
              )
```

This formula uses a lookup table (what we entered in the `Pop Index` Sheet) to search a range of data step by step and make substitutions. The `REDUCE` function is used to update the range of data (in the A column, `A1:A1914` - this will be all of the rows) for each step through the lookup table -- the steps through the lookup table are defined using the `SEQUENCE` and `ROWS` functions. So for each row of the lookup table, this function goes through all of the values in the range and substitutes the current value in the cell with the one from the lookup table (column B) if the current value if the search string (column A in the lookup table) is found within the cell. By wrapping this whole thing in `ARRAYFORMULA`, the looping action works, making substitutions across the whole column rather than just a single cell.

:::

:::

Second, using `Find and replace` from the Edit menu:

  * Try it out in your `COPY of ADMIXTURE_ancestry_props` sheet and see how the manual process is. Note that there's not a record of any changes it makes, while at least with the formulas above other people can look at the formulas after the fact.

::: {.emphasis_block}

Mutating or substituting values was important for changing the appearance of the population labels when working with the data in R.

:::

### Filtering out values

If you needed to filter out rows, you can again use a formula. The [`FILTER` function](https://support.google.com/docs/answer/3093197?hl=en) takes a range of data to copy (and filter while copying). Any parameters after that specify the conditions to evaluate to decide if a row of data is included or filtered out in the copy. So specifying something like `A:A = "Value"` will copy the data for rows where the A column has the value "Value". 

* Click the `+` (Add Sheet) button on the bottom left of the Google Sheet.
* Name the new sheet "Testing Filter 1".
* In A1 of the new sheet `Testing Filter 1`, enter the following formula that is specifying to filter the range to only include rows where the `pop` column is equal to (`=`) "Amhara":

```{.default}
=FILTER(ADMIXTURE_ancestry_props!A1:I1914, ADMIXTURE_ancestry_props!A1:A1914 = "Amhara")
```
* Note that the headers are not brought over in this example because A1 does not equal "Amhara".

* Click the `+` (Add Sheet) button on the bottom left of the Google Sheet.
* Name the new sheet "Testing Filter 2".
* In A1 of the new sheet `Testing Filter 2`, enter the following formula that is specifying to filter the range to only include rows where the `pop` column is **not equal** to (`<>`) "Amhara":

```{.default}
=FILTER(ADMIXTURE_ancestry_props!A1:I1914, ADMIXTURE_ancestry_props!A1:A1914 <> "Amhara")
```
* Note that the headers are brought over in this example because the A1 value does not equal "Amhara".

* Click the `+` (Add Sheet) button on the bottom left of the Google Sheet.
* Name the new sheet "Testing Filter 3".
* In A1 of the new sheet `Testing Filter 3`, enter the following formula that is specifying to filter the range to only include rows where the `pop` column is not (`<>`) "Amhara" and not "Zulu". The comma between the two conditions (not Amhara and not Zulu) acts as an "and" telling the function to evaluate both conditions when grabbing the data. (We could add more conditions separating them with commas and all of them would be enforced.):

```{.default}
=FILTER(ADMIXTURE_ancestry_props!A1:I1914, ADMIXTURE_ancestry_props!A1:A1914 <> "Amhara", ADMIXTURE_ancestry_props!A1:A1914 <> "Zulu")
```

::: {.emphasis_block}

Filtering out values was necessary for removing NAs when working with the data in R.

:::

### Ordering or Sorting

If you needed to order or sort values in a dataset, Google Sheets provides the built-in functionality of a menu tool as well as a function for formulas.

First, using the [`SORT` function](https://support.google.com/docs/answer/3093150?hl=en):

This function's parameters include a range of data (which will be sorted), the column that should be used to sort the data, and whether to use Ascending (TRUE) sort or Descending (FALSE) sort.

* Click the `+` (Add Sheet) button on the bottom left of the Google Sheet.
* Name the new sheet "Testing Sort 1".
* In A1 of the new sheet `Testing Sort 1`, enter the following formula that is specifying to grab all of the original data (minus the column names because it's starting at row 2) and sort based off of the population names with an Ascending sort (A --> Z)

```{.default}
=SORT(ADMIXTURE_ancestry_props!A2:I1914, ADMIXTURE_ancestry_props!A2:A1914, TRUE)
```

* Notice that "African American" sample "NA19625" is listed first.
* Edit the formula to switch the TRUE --> FALSE (descending sort Z --> A).
* Notice that the "Zulu" sample "EGAN00001068937" is now listed first and the estimated proportions of ancestry changed too, so the whole dataset is being sorted based on the population.

If you wanted to add the column names:

* Right click the "1" for row 1.
* Select "+ Insert 1 row above".
* Enter column header names in the new row.

::: {.question_block}

Why wouldn't we bring the column header names over in the sort function automatically?

<details><summary>Hint</summary>

Google Sheets doesn't know that the column names aren't regular data.

</details>

<details><summary>Answer</summary>

The "pop" column name would have been sorted along with the rest of the data and wouldn't have been the first row instead appearing in between the "Oromo" and "Rwandese" samples.

</details>

:::

Second, using the built-in Sort functionality / menu tool:

* In the `COPY of ADMIXTURE_ancestry_props` sheet, click the "View" menu option, followed by "Freeze", and "1 row".
* Click the "A" for the A column.
* Click the "Data" menu option, followed by "Sort sheet", and "Sort sheet by column A (A to Z)".
* Notice that again "African American" sample "NA19625" is listed first in row 2. Because we "froze" the column name row (row 1), those rows are not impacted by the sort.

Note that while the built-in sort functionality is easier to use, it is not as reproducible as the formula because you and others can't review exactly what was done.

What if you didn't want to sort alphabetically and instead wanted to use a specific ordering?

* Click the `+` (Add Sheet) button on the bottom left of the Google Sheet.
* Name the new sheet "Testing Sort 2".
* In A1 of the new sheet `Testing Sort 2`, paste the following order of populations:

```{.default}
Nama
Zulu
Mende
Esan
Yoruba
Mandinka
African Caribbean
African American
Banyarwanda
Banyankole
Bakiga
Barundi
Rwandese
Baganda
Luhya
Gumuz
Somali
Wolayta
Oromo
Amhara
Egyptian
Gujarati
Mexican
British
Italian
Han Chinese
```

* In C2 of the new sheet `Testing Sort 2`, paste the following formula:

```{.default}
=SORT(ADMIXTURE_ancestry_props!A2:I1914, MATCH(ADMIXTURE_ancestry_props!A2:A1914, A1:A26, 0), TRUE)
```

The addition of the [`MATCH` function](https://support.google.com/docs/answer/3093378?hl=en) to this formula finds the order of the populations in the original dataset according to the order they appear in the A column in the new sheet, looking for exact matches (0) and uses that to sort the whole of the original dataset.

If you scroll, you'll see that the populations are now in the specified order. You will need to bring the column names over for the data.

::: {.emphasis_block}

Ordering or sorting data was valuable for making the populations appear in the order we wanted for the plot in R.
:::

### Wide to Long Transformation

If you needed to rearrange or transform the data from a wide format to a long format, [this blog post explains a method to do that](https://www.benlcollins.com/spreadsheets/unpivot-in-google-sheets/).   

* Click the `+` (Add Sheet) button on the bottom left of the Google Sheet.
* Name the new sheet "Longer Data".
* Copy the following nested formula, double click in A1 of the new sheet (so that the cursor is in the formula bar), paste the nested formula, and press enter / return.


```{.default}
=ARRAYFORMULA(SPLIT(FLATTEN(ADMIXTURE_ancestry_props!B2:ADMIXTURE_ancestry_props!B1914&","
                            &ADMIXTURE_ancestry_props!A2:ADMIXTURE_ancestry_props!A1914&","
                            &ADMIXTURE_ancestry_props!C1:ADMIXTURE_ancestry_props!I1&","
                            &ADMIXTURE_ancestry_props!C2:ADMIXTURE_ancestry_props!I1914
                            ),
                    ",")
              )
```

This formula works by first concatenating data by putting ampresands (`&`) after and before data to be joined. The actual "character" coming in between the concatenated data is in quotes so a comma (`,`). First, the sample ID is joined with the population label which is then joined with the column names for the ancestry proportions, and then finally the ancestry proportions are concatenated. The FLATTEN function collapses the resulting combined strings into a single vertical column. The SPLIT function then splits the concatenated data at the character that separates the various parts of the data (the comma). As before the ARRAYFORMULA function allows this process to work for the whole dataset rather than one value.

```{r echo=FALSE}
ottrpal::include_slide("https://docs.google.com/presentation/d/1fmPgoGk8KCZvRQx9ljjti8Nn44JdA2G2yGm1BwmL7V8/edit?slide=id.g3fa5dc926b3_0_10#slide=id.g3fa5dc926b3_0_10")
```

After the `FLATTEN` part of the formula, for each sample, there will be 7 rows of 1 column data instead of 7 columns of 1 row of data. The ID and population part of the data will be the same for all 7 rows, but the ancestry proportion label (`k1`, etc.) will be different for all 7 rows. The corresponding estimated ancestry proportion (a decimal number) will not necessarily be the same for all 7 rows (though some may have the same numbers when compared to each other).

After the `SPLIT` part of the formula, for each sample, there will be 7 rows of 4 column data with the ID, population, ancestry proportion label (`k1`, etc.), and estimated ancestry proportion all being in their own columns.

The column names were not brought over. If you wanted to add them:

* Right click the "1" for row 1.
* Select "+ Insert 1 row above".
* Enter column header names in the new row.

Unlike in R, Google Sheets uses data from a wide format when making graphs, so transforming wide data to long data is not necessary for the data visualization steps of this case study. 

:::

# **Data Visualization**

------------------------------------------------------------------------

:::{.panel-tabset}

## R

If you have been following along but stopped, we could load our wrangled data like so:

```{r, eval = FALSE}
load(here::here("data", "wrangled", "ADMIXTURE_ancestry_props.Q"))
```


To visualize our data, we will first need to make it into a form called long form. This will allow us to work with all of the k columns at once. To do so we will use a function called `pivot_longer`. we need to specify what columns to pivot and what to name the new column where the values will go and what to name the column were the original column names will be used as values. 

```{r}
ancestry_long <- ancestry_props %>%
  pivot_longer(cols = starts_with("k"), values_to = "prop", names_to = "which_k")

```

Let's check our work with `head` again.

```{r}
head(ancestry_long)

```

Nice now we have all our k values in one column and another column indicating what k it came from. 

Now we will use this to make a plot like the one in the paper. We will use the `ggplot` function of the `ggplot2` package. This is famous R package for making visualizations. First we need to specify our aesthetics with the `aes` function to indicate what data will be on what axis and how we might color or group our data. In our case we want `id`(which is the gene name)  on the y axis, `prop` on the x axis and we want to color by what k it came from (`which_k`).  We will create a bar plot so we need to specify that with the `geom_bar` function. We need to tell R to plot the raw values of y, and we can do that using the `stat = "identity"` argument. Otherwise, it would count the number of rows for unique values in x. 

```{r}
 ancestry_long %>%
  ggplot(aes(y=id,
             x=prop,
             fill = factor(which_k)))+
  geom_bar(stat = "identity") 

```


OK not bad! We have made a plot that kinda looks like what we are trying to recreate pretty fast. However, we can do better. We have some white lines between our rows of data on the plot. Our y axis isn't readable and we have different colors than the original plot. 

```{r fig.width = 5, fig.height = 10}
plot1 <- ancestry_long %>%
  ggplot(aes(y=id,
             x=prop,
             fill = factor(which_k),
             group = pop)) +
  geom_bar(stat = "identity", position = "stack", width = 1) +
  facet_col(~pop,
             #ncol=1, #included in facet_wrap, but not facet_col
             strip.position = "left",
             scales = "free_y",
             space = "free") + #argument in facet_col, but not facet_wrap -- better reflects how many samples are in the facet in my opinion
  theme(axis.text.y = element_blank(),
        strip.text.y.left = element_text(angle = 0),
        strip.background = element_blank(),
        legend.position = "none") +
  ylab("") +
  scale_fill_manual(breaks = c("k6", "k7", "k1", "k4", "k5", "k3", "k2"),
                    values = c("#2B284B", "#FFE6AD", "#30A9E1", "#056EB5", "#2BA06A", "#EC5D25", "#A41252"))

```

```{r, fig.width = 2, fig.height=10, message=FALSE, warning=FALSE}
toPlot <- left_join(ancestry_props_temp %>%
            select(!starts_with("k")) %>%
            rownames_to_column(),
          ancestry_props_temp %>%
            select(starts_with("k")) %>%
            mutate(which_k = names(.)[max.col(.)]) %>%
            rownames_to_column() %>%
            select(rowname, which_k),
          by="rowname") %>%
  group_by(pop, which_k) %>%
  summarize(count = n()) %>%
  ungroup() %>%
  group_by(pop) %>%
  top_n(1, count) %>%
  left_join(ancestry_props_temp %>%
               group_by(pop) %>%
               summarize(n_ids = n()), by = "pop")

toPlotExpanded <- toPlot[rep(seq_len(nrow(toPlot)), toPlot$n_ids), c("pop", "which_k")]

plot2 <- toPlotExpanded %>%
  ggplot(aes(x = -1,
             y = row.names(.),
             fill = which_k)) +
  geom_col(width=1) +
  facet_col(~pop,
            scales = "free_y",
            space = "free"
            ) +
  theme_bw() +
  theme(axis.text = element_blank(),
        axis.ticks = element_blank(),
        legend.position = "none",
        strip.background = element_blank(),
        strip.text.x = element_blank()) +
  xlab("") +
  ylab("") +
  xlim(c(-1,0)) +
  scale_fill_manual(breaks = c("k6", "k7", "k1", "k4", "k5", "k3", "k2"),
                    values = c("#2B284B", "#FFE6AD", "#30A9E1", "#056EB5", "#2BA06A", "#EC5D25", "#A41252"))
```

```{r fig.width = 5.5, fig.height = 12}
plot1 + plot2 +
  plot_layout(widths = c(5,0.5), heights=c(12))
```

## Google Sheets

Now we will use the imported data to make a plot like the one in the paper.

* Navigate to the original "ADMIXTURE_ancestry_props" sheet if you are not already there. 
* Select Columns A-I (the original data we imported).
* Click the "Insert" menu option, then "Chart".

Google Sheets will guess on what kind of chart you want and add it. It's going to look like a bar graph of sorts with thin, vertical strips of color representing the estimated ancestry proportions. The different colors represent the different ancestry clusters (k1, k2, etc.). Along the x-axis each sample is represented uniquelly and labeled with their population (though not every sample is labeled). And there's a white bar in the middle with the sample IDs. 

We will explore the "Setup" of this chart and "Customize" its appearance a little.

Double click on the chart and the "Chart editor" panel will be opened on the right-hand side of the screen.

* Notice that the "Chart type" is specified as "Column chart" which is we want. Google Sheets guessed well (mostly). 
* However, we want stacking, and "Stacking" is set to "None" currently. Right now it appears that the white we see in the chart is becasue it's plotting each cluster for each sample with their own vertical line. 
* Switch the "Stacking" selection from "None" to "100%". Now the vertical white strips are not visible in the chart and the Chart type was automatically updated to "100% stacked column chart". Every sample has a single vertical line where the proportions for each of the ancestry clusters is represented in that vertical line.
* The X-axis shows "pop" with an additional label for "id". CLick the circled "x" next to the "id" label to remove it. Now the chart doesn't have that white bar in the middle with the sample IDs. 
* If you click the "Aggregate" checkbox, instead of plotting a separate vertical line for each sample, it will aggregate the data for all samples in a given "pop" (by default summing but can switch to averaging) and plot a single bar for each population. While this is cool to see, it's not what the commentary's plot looks like, so don't leave the "Aggregate" option checked.
* The "Series" section lists k1, k2, k3, k4, k5, k6, and k7 all as their own series.

We can customize the plot to appear closer to the original plot. 

* Click the "Customize" tab of the Chart editor panel.
* Under "Series", select "k1" from the dropdown.
* For Fill color, select the plus sign (`+` "add a custom color") and enter #30A9E1 in the "Hex" box.
* Click the green "OK" button.
* Using this table to find the appropriate Hex value, repeat the process for the remaining series (k2, k3, k4, k5, k6, and k7).

| Cluster | Hex |
| :----   | :-------: |
| k2      | #A41252 |
| k3      | #EC5D25 |
| k4      | #056EB5 |
| k5      | #2BA06A |
| k6      | #2B284B |
| k7      | #FFE6AD |

Notice that you can drag the edges of the chart to expand the size of it -- making it wider and taller. 

For the 3 remaining ways that the plot looks different than the commentary plot, we can discuss what you would do:

1. The commentary plot has horizontal lines instead of vertical lines. 

* Go back to the "Setup" tab of the "Chart editor".
* For "Chart type", scroll down to the "Bar" section and select the furthest right option that shows stacked bars the full width of the plot.
* Back on the Setup tab "Chart type" should say: "100% stacked bar chart".

2. The population order does not match.

* Recreate these graphing steps in the `Testing Sort 2` sheet (from the wrangling section). The population order should match at the end of it.

3. Populations are named more than once 

* There is nothing we can directly do for this plot to fix the issue. However, we can make a separate plot for each population and later combine them. This is a time consuming and repetitive process. For sake of the example, we will do this twice.
* Navigate to the `Testing Filter 1` sheet (from the wrangling section). 
* Create a chart with the data:
  * Chart type "100% stacked bar chart"
  * For the Y-axis "pop" within the "Setup" tab of the "Chart editor" panel, click the three dots and "Remove" to remove the population labels.
  * Update the Series colors in the "Customize" tab
  * Add "Amhara" under the "Title text" option of the "Chart & axis titles" section of the "Customize" tab.

* Repeat the filtering process from the Data Wrangling section together with the building the chart process for the "Nama", "Rwandese", and "Han Chinese" populations all separately.

:::{.emphasis_block}

If you repeated this process for all 26 populations, you could combine the plots together to more closely recreate the plot from the commentary, but that requires a lot of manual work and repeated steps.

:::

:::

# **Analysis**

------------------------------------------------------------------------

::: {.panel-tabset}

## R

Finding the average proportion of each cluster for each population.

## Google Sheets

Finding the average proportion of each cluster for each population.

:::

# **Summary**

------------------------------------------------------------------------

# **Suggested Homework**

------------------------------------------------------------------------

# **Additional Information**

## **Glossary**

------------------------------------------------------------------------

```{r}
#| results: asis
#| echo: false

library(yaml)

glossary_table <- function(path = "glossary.yml") {

  g <- yaml::read_yaml(path)
  g_sorted <- g[order(names(g))]

  out <- c(
    "<table class='glossary_table'>",
    "<tr><th><b>Term</b></th><th><b>Definition</b></th></tr>"
  )

  for (term in names(g_sorted)) {
    def <- paste(g_sorted[[term]], collapse = "\n")
    row <- sprintf("<tr><td>%s</td><td>%s</td></tr>", term, def)
    out <- c(out, row)
  }

  out <- c(out, "</table>")

  knitr::asis_output(paste(out, collapse = "\n"))
}

glossary_table("glossary.yml")

```

## **References**

------------------------------------------------------------------------

::: {#refs}
:::

## **Helpful Links**

------------------------------------------------------------------------

Add additional resource links here
